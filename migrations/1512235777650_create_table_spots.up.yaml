- args:
    sql: CREATE TABLE "spots"("id" integer NOT NULL, "name" text NOT NULL, "pop" integer
      NOT NULL, "tc" integer NOT NULL, "time" integer NOT NULL, PRIMARY KEY ("id")
      )
  type: run_sql
- args:
    name: spots
  type: add_existing_table_or_view
